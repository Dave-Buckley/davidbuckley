import { describe, it, expect } from 'vitest'
// Plan 02 implements: import { getPhases, getProjects } from './content'

describe.skip('getPhases (Plan 02)', () => {
  it('returns phases sorted by number ascending', () => {
    // const phases = getPhases()
    // expect(phases.map(p => p.number)).toEqual([1, 2, 3, 4, 5, 6])
    expect(true).toBe(false)
  })
})

describe.skip('getProjects (Plan 02)', () => {
  it('returns only entries with required frontmatter (title, phase, status, summary)', () => {
    // const projects = getProjects()
    // projects.forEach(p => {
    //   expect(p.title).toBeDefined()
    //   expect(p.phase).toBeDefined()
    //   expect(p.status).toBeDefined()
    //   expect(p.summary).toBeDefined()
    // })
    expect(true).toBe(false)
  })
})
